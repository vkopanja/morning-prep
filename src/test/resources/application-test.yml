spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/morningprep_test
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
scheduler:
  enabled: false
logging:
  level:
    com.usergems: INFO
